import React from 'react';

const Header = () => (
    <div>
    <div className="container">
      <h1 className="header center white-text">Clicky Game!</h1>
      <div className="row center">
        <h5 className="header col s12 light white-text">Click on an image to earn points, but don't click on any more than once!</h5>
      </div>
    </div>
  </div>
);

export default Header;